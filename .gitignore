node_modules/

# Hồ sơ trình duyệt: chứa cookie phiên đăng nhập LinkedIn — không bao giờ commit
.browser-profile/

# File capture: JSONL đã che header nhạy cảm, nhưng HAR thì CHƯA — giữ ngoài git cho chắc
captures/

# Bản sao lưu resume.yaml do dashboard tạo tự động (xoay vòng 10 bản)
data/backups/

# Sản phẩm trung gian của build (sinh lại được bằng npm run build)
output/preview.html
output/cv-style.css
output/*.png
# Font Times đã tô đậm: dựng lại được bằng tools/build-faux-bold-font.py.
# KHÔNG commit - phát hành lại file font Windows là vi phạm giấy phép.
output/*.woff2

# Bytecode Python sinh ra khi chạy tools/
__pycache__/

# File đo đạc tạm thời khi đối chiếu với bản CV gốc
_measure.mjs
